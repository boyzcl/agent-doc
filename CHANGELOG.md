# Changelog

## 0.1.0 - 2026-04-28

- Introduced a local-first runtime evolution layer for `agent-doc`.
- Added runtime specifications for memory, promotion policy, host abstraction, and operator quickstart.
- Added minimal runtime helper scripts for init, validate, capture, read, promotion, governance reporting, repo candidate review, and smoke testing.
- Integrated runtime guidance into `README.md`, `SKILL.md`, and `agents/openai.yaml`.
- Added a reusable sample capture for real-host runtime validation.
- Validated the runtime flow with temporary-root smoke tests and a real Codex host runtime run.
